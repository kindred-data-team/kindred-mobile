import 'package:flutter/material.dart';

class CarePlanNotesCard extends StatelessWidget {
  const CarePlanNotesCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
